// Generated by CoffeeScript 1.7.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['jquery', 'underscore', 'backbone', 'mediadata', 'text!templates/contact.html'], function($, _, Backbone, md, tplContact) {
    'use strict';
    var ContactView;
    return ContactView = (function(_super) {
      __extends(ContactView, _super);

      function ContactView() {
        return ContactView.__super__.constructor.apply(this, arguments);
      }

      ContactView.prototype.el = '#main';

      ContactView.prototype.collection = null;

      ContactView.prototype.template = _.template(tplContact);

      ContactView.prototype.name = null;

      ContactView.prototype.initialize = function() {
        return this.render();
      };

      ContactView.prototype.destroy = function() {};

      ContactView.prototype.render = function() {
        document.body.scrollTop = document.documentElement.scrollTop = 0;
        this.$el.html(this.template());
        md.Router.hideLoader();
        return this;
      };

      return ContactView;

    })(Backbone.View);
  });

}).call(this);

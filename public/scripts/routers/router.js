// Generated by CoffeeScript 1.7.1
(function() {
  define(['mediadata'], function(MD) {
    'use strict';
    return Backbone.Router.extend({
      routes: {
        '': 'home',
        'explorer': 'explorer',
        ':person': 'setFilter',
        ':person/:otherPerson': 'compare'
      },
      initialize: function() {
        Backbone.history.start({
          pushState: true,
          root: '/mediadata/public/'
        });
        return this.onResize();
      },
      compare: function(person, otherPerson) {
        return console.log(person, otherPerson);
      },
      explorer: function() {
        console.log('explorer');
        return this.render('explorer');
      },
      home: function() {
        console.log('home');
        return this.render('home');
      },
      setFilter: function(param) {
        return console.log(param);
      },
      onResize: function() {
        return document.getElementById('main').style.width = window.innerWidth - 275 + 'px';
      },
      render: function(view) {
        MD.Status.previousView = MD.Status.currentView || '';
        MD.Status.currentView = view;
        return this.createView(view);
      },
      createView: function(view) {
        return require(['views/' + view], function(View) {
          return MD.Views[view] = new View();
        });
      }
    });
  });

}).call(this);

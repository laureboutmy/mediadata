// Generated by CoffeeScript 1.7.1
(function() {
  var height, margin, person1, person2, stackedObj, width, x, y;

  person1 = 'anne-hidalgo';

  person2 = 'christiane-taubira';

  margin = {
    top: 60,
    right: 20,
    bottom: 60,
    left: 40
  };

  width = 1020;

  height = 355;

  x = d3.scale.ordinal().rangeRoundBands([0, width], .1);

  y = d3.scale.linear().range([height, 0]);

  stackedObj = [];

  d3.json('person-' + person1 + '.json', function(error, data1) {
    if (error) {
      return console.warn(error);
    }
    d3.json('person-' + person2 + '.json', function(error, data2) {
      var k, v, _ref, _results;
      if (error) {
        return console.warn(error);
      }
      _ref = data1.channels;
      _results = [];
      for (k in _ref) {
        v = _ref[k];
        console.log(v.channelName);
        stackedObj.push(v.channelName);
        _results.push(console.log(stackedObj));
      }
      return _results;
    });
  });

}).call(this);
